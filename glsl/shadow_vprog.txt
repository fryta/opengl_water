#version 330

in vec3 point;

out vec3 pointWorld;

uniform mat4 model;
uniform mat4 modelView;
uniform mat4 proj;


void main()
{
	pointWorld  = (model*vec4(point, 1.0)).xyz;
	gl_Position = proj*modelView*vec4(point, 1.0);
}
